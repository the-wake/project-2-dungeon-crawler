<h2>Update campaign name</h2>
<div class="container">


    <form action="/api/campaign/" method="POST">
        {{!-- <div class="mb-3">
            <label for="campaign-id" class="form-label">What is the campaign id that you want to update?</label>
            <input name="id" type="text" class="form-control" id="campaign-id" aria-describedby="campaign-id">
        </div> --}}

        <div class="dropdown">
            <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true"
                aria-expanded="true">
                Dropdown
                <span class="caret"></span>
            </button>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                {{#each campaigns as |campaign|}}
                <a class="dropdown-item" href="#">{{campaign.name}}</a>
                {{/each}}
            </div>
        </div>

        <div class="mb-3">
            <label for="campaign-name" class="form-label">What is your new Campaign name?</label>
            <input name="originalname" type="text" class="form-control" id="campaign-name"
                aria-describedby="campaign-name">
        </div>
        <input type="text" name="updatedname">
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>